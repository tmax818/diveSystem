
int main()
{
    /* define3 variables in this block's scope at the top of the block */
    int x;

    int i, j, k;

    char letter; // a char stores a single-byte integer value it is often used to store a single ASCII character value (ASCII numeric encoding of a char) a char in C is a different type than a string in C

    float winpct; 
    double pi;
}
